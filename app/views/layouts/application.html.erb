<!DOCTYPE html>
<html>
        <body style="background-color:MediumSpringGreen;">

  <head>
    
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Rails Signup Download" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Rails Signup Download" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
    
    <style>
      

    </style>
    
    
  </head>
  <body>
    
    <main role="main">
       <%= render 'layouts/messages' %>
       
    
       
       
       
       <!-- navbar can go here because it will be on every page of the app.  -->
<header>
        <%= render 'layouts/navigation' %> 
    </header>
       <%= yield %>
    </main>
  </body>
</html>
